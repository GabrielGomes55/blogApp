<div class="jumbotron">

  <h1 class="display-4">Bem vindo ao blog do node</h1>
  <p class="lead">Blog simples desenvolvido com nodejs, express e mongodb.</p>
  <hr class="my-4">
  <p>Crie uma conta para ficar por dentro de tudo que acontece no mundo da computação</p>
  <p class="lead">
    {{#if user}}
      {{else}}
      <a class="btn btn-primary btn-lg" href="/usuarios/registro" role="button">Crie uma conta</a>
    {{/if}}
  </p>
</div>

<hr>

<h2>Postagens recentes</h2>

{{#each postagens}}

    <div class="card mt-4">
        <div class="card-body">
            <h4>{{titulo}}</h4>
            <p>{{descricao}}</p>
           <a href="/postagem/{{slug}}"><button class="btn btn-info">Leia mais</button></a>
            <hr>
            <small>{{data}}</small><br>
            <small>Categoria: {{categoria.nome}}</small>
            <br>
        </div>
    </div>

{{else}}
<h5>nenhuma postagem</h5>
{{/each}}